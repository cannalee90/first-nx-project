name: Trigger foundation upgrade
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'foundation version for upgrade'
        default: '0'
        required: true

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "Hello upgrade to ${{ github.event.inputs.version }}"
